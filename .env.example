# Private key of the deployment wallet (must be the same for both chains)
PRIVATE_KEY=0x123abc...

# Address that will own the contracts (project owner)
DELEGATE_ADDRESS=0x...

# Address that will receive all tokens on BSC
TREASURY_ADDRESS=0x...

# RPC URLs for LayerZero setup
BSC_RPC_URL=https://bsc-dataseed.binance.org/
ETH_RPC_URL=https://ethereum.publicnode.com

# API keys for verification
BSCSCAN_API_KEY=
ETHERSCAN_API_KEY= 